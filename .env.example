# Variables usadas por docker-compose y los servicios locales

# Password del usuario "qrpass" en Postgres (usado tanto afuera como dentro del contenedor)
DB_PASSWORD=qrpass123

# Secret para firmar los JWT de la API (cambiá esto en producción)
JWT_SECRET=dev-super-secret-please-change

# URL pública de la API; la web la usa para apuntar al backend
API_URL=http://localhost:3000
