generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: definir modelos al migrar desde QRPass original
// Los modelos se irán agregando a medida que se migre cada módulo
